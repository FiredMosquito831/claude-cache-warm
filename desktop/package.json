{
  "name": "claude-cache-warm-desktop",
  "private": true,
  "version": "0.1.0",
  "description": "Tray app for Claude Cache Warm (Tauri v2). The dashboard is the real UI.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build --config src-tauri/tauri.bundle.conf.json",
    "icon": "python scripts/make-icon.py && tauri icon app-icon.png"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
